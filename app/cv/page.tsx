import da from "@/content/da.json"; import en from "@/content/en.json"; import Section from "@/components/Section";
export default function CV({ searchParams }:{ searchParams:{ lang?: string } }){
  const lang = searchParams?.lang === "en" ? "en" : "da"; const data = lang === "en" ? en : da;
  return (<><section className="bg-hero-gradient"><div className="max-w-5xl mx-auto px-4 py-12 flex flex-wrap items-end gap-6"><div><h1 className="text-3xl font-bold">{data.name}</h1><p className="text-lg text-white/80">{data.title}</p><div className="mt-4 flex gap-3"><a className="btn glow" href="/Elisabeth_Lennert_CV.pdf" download>Download CV (PDF)</a><a className="btn" href={data.links.github} target="_blank">GitHub</a><a className="btn" href={data.links.linkedin} target="_blank">LinkedIn</a></div></div></div></section><Section title={lang === "en" ? "Highlights" : "Højdepunkter"}><div className="flex flex-wrap gap-2">{(data as any).badges.map((b:string)=>(<span key={b} className="badge">{b}</span>))}</div></Section><Section title={lang === "en" ? "Skills" : "Færdigheder"}><div className="flex flex-wrap gap-2">{["React","Next.js","TypeScript","Node.js","Docker","Kubernetes","Firebase","C#","Python","SQL"].map(s=>(<span key={s} className="badge">{s}</span>))}</div></Section><Section title="Contact"><ul className="space-y-1 text-white/80"><li><a className="underline" href={(data as any).links.email}>Email</a></li><li><a className="underline" href={(data as any).links.github} target="_blank">GitHub</a></li><li><a className="underline" href={(data as any).links.linkedin} target="_blank">LinkedIn</a></li></ul></Section></>);
}
